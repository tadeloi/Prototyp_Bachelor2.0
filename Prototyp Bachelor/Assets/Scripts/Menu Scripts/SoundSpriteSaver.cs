using System;
using UnityEngine;

public class SoundSpriteSaver : MonoBehaviour
{

    [SerializeField] public Sprite offSprite;
    [SerializeField] public Sprite musicSprite;
    [SerializeField] public Sprite backgroundSprite;
    [SerializeField] public Sprite bothSprite;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
